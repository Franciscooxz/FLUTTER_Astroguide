name: astroguide_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0  # Agregamos la dependencia del paquete http
  google_fonts: ^6.1.0
  get: ^4.6.6
  get_storage: ^2.1.1
  iconsax: ^0.0.8
  go_router: ^13.2.3
  logger: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/

